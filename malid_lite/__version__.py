"""Mal-ID-Lite version information."""

# __version__ = "0.1.0" # Used internal data format (raw data from synapse). CV support only, TCR only.
# __version__ = "0.1.1" # Uses airr data format (clean version).
__version__ = "0.1.2" # Added support for training on a full dataset, and testing on another dataset.
