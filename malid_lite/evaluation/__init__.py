"""External evaluation for Mal-ID-Lite.

Evaluate a trained model (ensemble or standalone base models) on a separate
dataset — the "test side" of the pipeline replayed on external data. See
``evaluate_external.py`` for the orchestration + CLI and
``evaluate_external_reporting.py`` for metrics/figures/report rendering.
"""
